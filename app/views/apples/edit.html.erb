<!-- Validation failure messages -->
<% if @apple.errors.any? %>
  <% @apple.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Apple #<%= @apple.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_apple/<%= @apple.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @apple.name %>">
      </div>

      <button class="btn btn-warning">
        Update Apple
      </button>
      or
      <a href="/apples/<%= @apple.id %>">Cancel</a>
    </form>
  </div>
</div>
